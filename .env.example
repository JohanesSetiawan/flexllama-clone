# Router Model API Environment Variables
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Required: API Key for B2B authentication
# Used in Authorization: Bearer <API_KEY> header
API_KEY=your_secret_api_key_here

# Optional: Redis URL (defaults to docker-compose service)
# REDIS_URL=redis://localhost:6379/0

# Optional: Path to models directory (for docker-compose volume mount)
# MODELS_PATH=/path/to/your/models
