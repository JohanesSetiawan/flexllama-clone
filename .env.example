# Router Model API Environment Variables
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# API Key for B2B (Backend to Backend) authentication
# If set, requests must include Authorization: Bearer <API_KEY> header
# If not set or empty, authentication is DISABLED
# API_KEY=your_secret_api_key_here

# Redis URL for rate limiting (defaults to docker-compose service)
# REDIS_URL=redis://localhost:6379/0

# Path to models directory (for docker-compose volume mount)
# MODELS_PATH=/path/to/your/models
